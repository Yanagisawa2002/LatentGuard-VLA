# LG-RB0.1 next step

## Formal stop

The RoboLab counterfactual-platform route is stopped at Result C. The permitted
single compatibility patch repaired recorded-config callable preservation, but
the required three-repeat faithful gate failed with repeat-dependent state
drift. A second compatibility-patch milestone is explicitly prohibited.

`LG_RB1_AUTHORIZED=false`.

## What must not happen

- Do not run prefix, same-suffix, branch-isolation, or takeover experiments.
- Do not add restore fields, broaden canonicalization, or relax tolerances.
- Do not alter task code, physics, predicates, actions, or the ten recordings.
- Do not collect replacement recordings selected for easier replay.
- Do not connect a VLA, policy candidate, ranker, world model, failure head, or
  intervention logic.
- Do not train a model or access final seeds.
- Do not submit the prepared upstream issue or PR text without separate user
  authorization.

## Decision needed for a future milestone

Any continuation requires a user-authorized route outside this stopped
counterfactual platform path. A future design should begin with an execution
platform that natively exposes a reviewed, complete reset/state contract and
must prove repeated same-action replay before policy work. It should not treat
the ten first-repeat passes, terminal agreement, or success agreement as
permission to bypass the failed state gate.

The server has been left powered on and SSH-ready. No further remote job is
running.
