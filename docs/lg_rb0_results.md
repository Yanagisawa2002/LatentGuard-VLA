# LG-RB0 results

Status: remote execution pending.

The protocol and CPU-only validation contracts are frozen before RoboLab
execution. No result is assigned until all required compact artifacts have
been retrieved, validated, and reviewed. Unrun metrics are unavailable, not
zero.

The final update must report:

- exact local and external commits;
- environment and one-step smoke result;
- ten-recording manifest;
- 30 faithful replay runs;
- early/middle/late prefix repeats;
- A/B fixed-suffix determinism;
- A-B-A and B-A-B isolation;
- semantic coverage;
- Result A, B, or C and `LG_RB1_AUTHORIZED`;
- explicit confirmation that no policy/ranker training or final-seed access
  occurred.

LG-R2b0 Result C remains frozen and unchanged. LG-RB0 is a separate simulator
feasibility route and cannot reinterpret the LIBERO evidence.
