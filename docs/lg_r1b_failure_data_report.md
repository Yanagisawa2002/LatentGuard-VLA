# LG-R1b natural failure data report

Status: task/seed registry frozen; natural rollout evidence pending.

Only failures from the unchanged policy on pre-registered standard LIBERO
tasks count. Infrastructure errors, synthetic corruptions, deliberately weak
checkpoints, and modified horizons do not count. The completed report will
include task and taxonomy distributions, first abnormal steps, matched-window
coverage, leakage checks, and any low-yield stop decision.
