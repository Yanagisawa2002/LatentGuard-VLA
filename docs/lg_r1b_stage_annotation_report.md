# LG-R1b stage annotation report

Status: protocol frozen; remote episode evidence pending.

The task adapters and QA thresholds are defined in
`configs/lg_r1b/stages.yaml`. The completed report will state reviewed
success/failure episodes per task, critical errors, minor disagreement,
deterministic mismatch, observed regressions, label limitations, and the
machine-readable artifact hash. No stage uses elapsed time as its semantic.
