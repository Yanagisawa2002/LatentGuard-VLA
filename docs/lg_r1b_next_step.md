# LG-R1b next-step decision

Status: pending the pilot, zero-shot generalization result, natural-failure
yield, and LG-R2 gate.

If standard LIBERO yields fewer than ten natural failures after both
pre-registered groups and 200 episodes, the default recommendation is to keep
LIBERO as a progress benchmark and move later failure/intervention research to
a harder benchmark. This milestone does not execute RoboLab and does not
return to synthetic action corruption, bounded PickCube ACT, or LangMani.
