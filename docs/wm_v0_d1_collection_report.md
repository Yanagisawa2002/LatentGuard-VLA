# WM-v0 D1 collection report

## Current status

**Result B in progress; formal training is not authorized.** The six accepted
baseline futures remain reproducible and immutable. The new D1 transactional
collection currently contains zero committed samples pending an exact pushed
revision and bounded remote pilot.

The required real-policy provider is blocked: the active server has no accepted
LangMani controller registry, runtime-selection record, ACT checkpoint, or
processor artifacts. M6A.1 performed zero policy queries. No historical action
snippet, Gaussian perturbation, constant bias, or manually edited chunk will be
relabeled as policy-generated.

## Implemented collection contract

- outcome-free typed policy candidate interface;
- mandatory checkpoint SHA-256 for real policy candidates;
- deterministic action-content hashes and per-anchor deduplication;
- phase, progress, time, distance, grasp, object, episode, scene, and split
  metadata, with explicit unknown values;
- strict rejection reason codes and durable rejection log;
- one atomic directory per complete anchor with sample pairs, candidate metadata,
  sample index, manifest, and completion marker;
- quarantine and safe rerun of incomplete anchors;
- complete-anchor zero-work resume;
- strict pilot and formal gates in the manifest and quality report; and
- no contribution from synthetic candidates to the 70% real-policy quota.

## Gate status before pilot

The committed fail-closed D1 artifacts report zero D1 samples, anchors, episodes,
successes, failures, and policy-generated candidates. Therefore both the pilot and
formal gates are false. This is an initialization state, not a claim that the six
historical samples disappeared; they remain under the accepted WM-v0 smoke
artifacts and will only join a D1 dataset through an explicit provenance-preserving
import or re-collection.

The next authorized execution is a 25--50 anchor, approximately 120-future,
single-GPU mechanics pilot using exact PickCube restoration. If only frozen M3C
synthetic candidates are available, it will validate throughput, recovery,
deduplication, future alignment, event/progress labels, and class distribution,
then deliberately fail the real-policy ratio gate and stop. Formal 1,000-sample
collection and all formal model training remain prohibited until a verified policy
provider supplies at least 70% of candidates and every other pilot gate passes.

## Repair path for the real-policy blocker

1. Restore the accepted LangMani registry, runtime-selection record, ACT
   checkpoint, and MEAN_STD processor artifacts on the execution server.
2. Verify every file digest against a pushed, reviewed binding without importing
   LangMani into the LatentGuard core.
3. Run one reset-only policy query and confirm action shape, mask, bounds,
   deterministic seed behavior where supported, and checkpoint identity.
4. Bind the verified out-of-process provider to D1 and rerun the 120-sample pilot.
5. Continue only if policy-generated ratio, both-class coverage, completeness,
   leakage, duplicate, restoration, rejection, and cost gates all pass.

No evidence currently supports a formal future-latent advantage, model comparison,
or readiness for a selective closed-loop probe.
